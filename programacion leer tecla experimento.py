import pygame

pygame.init()

codigo_a = pygame.key.key_code("a")
print(codigo_a)  # Salida: 97 (que es el código para la tecla 'a')

codigo_espacio = pygame.key.key_code("space")
print(codigo_espacio)  # Salida: 32 (código ASCII para espacio)



#mirar en casa el pygame.key.key_code
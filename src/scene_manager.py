import pygame
import menu  # Import the menu module

class SceneManager: # Create a Game class to manage scene state
    def __init__(self):
        self.engine = engine
        self.current_scene = None

    def load_scene(self, scene_name):
    scene_maganer.load_scene("Menu") # Load the menu scene